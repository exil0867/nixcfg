result
// Direnv
**/.direnv
# Ignore everything in the secrets directory
secrets/*
secrets-sync/*

# Except for the secret.nix file
!secrets/secret.nix
secrets/*
!secrets/*.nix

!secrets-sync/secret.nix
secrets-sync/*
!secrets-sync/*.nix

